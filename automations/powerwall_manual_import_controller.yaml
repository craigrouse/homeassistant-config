id: powerwall_manual_import_controller
alias: 'Powerwall: Manual Import Controller'
description: 'Enables manual force import for X hours using input_number duration, pushing a 3-period TOU import tariff (isolating
  off-peak to duration window) and performing a 3m Kickstart nudge if battery is idle. 2026-09-02: tariff-first ordering (import
  TOU push, 15s settle, then grid switch, reserve 100, export pv_only, autonomous) because the gateway checks the tariff before
  honouring reserve/mode; kickstart toggle spacing 5s -> 45s to match the Export Watchdog (5s raced Tesla cloud sync). The
  kickstart fired correctly at 22:57 tonight and charging began ~90s later.'
triggers:
- entity_id:
  - input_boolean.start_manual_force_import
  id: start
  to: 'on'
  trigger: state
- entity_id:
  - input_boolean.start_manual_force_import
  id: stop
  to: 'off'
  trigger: state
- entity_id:
  - timer.import_countdown_timer
  id: timer_finished
  to: idle
  trigger: state
conditions: []
actions:
- choose:
  - conditions:
    - condition: trigger
      id:
      - start
    sequence:
    - action: timer.start
      data:
        duration: '{{ (states(''input_number.import_duration_hours'') | float(1.0) * 3600) | int }}'
      target:
        entity_id: timer.import_countdown_timer
    - action: teslemetry.time_of_use
      data:
        device_id: dd527e4bc7ab1a14f356f9973480e7f7
        tou_settings: |-
          {% set now_dt = now() %}
          {% set dur_hours = states('input_number.import_duration_hours') | float(1.0) %}
          {% set start_dt = now_dt | as_local %}
          {% set end_dt = (now_dt + timedelta(hours=dur_hours)) | as_local %}
          {% set start_hour = start_dt.hour | int %}
          {% set start_minute = start_dt.minute | int %}
          {% set end_hour = end_dt.hour | int %}
          {% set end_minute = end_dt.minute | int %}

          {% if end_dt.day == start_dt.day %}
            {% set off_peak_periods = [
              {'fromDayOfWeek': 0, 'fromHour': start_hour, 'fromMinute': start_minute, 'toDayOfWeek': 6, 'toHour': end_hour, 'toMinute': end_minute}
            ] %}
            {% set on_peak_periods = [
              {'fromDayOfWeek': 0, 'fromHour': 0, 'fromMinute': 0, 'toDayOfWeek': 6, 'toHour': start_hour, 'toMinute': start_minute},
              {'fromDayOfWeek': 0, 'fromHour': end_hour, 'fromMinute': end_minute, 'toDayOfWeek': 6, 'toHour': 0, 'toMinute': 0}
            ] %}
          {% else %}
            {% set off_peak_periods = [
              {'fromDayOfWeek': 0, 'fromHour': start_hour, 'fromMinute': start_minute, 'toDayOfWeek': 6, 'toHour': 0, 'toMinute': 0},
              {'fromDayOfWeek': 0, 'fromHour': 0, 'fromMinute': 0, 'toDayOfWeek': 6, 'toHour': end_hour, 'toMinute': end_minute}
            ] %}
            {% set on_peak_periods = [
              {'fromDayOfWeek': 0, 'fromHour': end_hour, 'fromMinute': end_minute, 'toDayOfWeek': 6, 'toHour': start_hour, 'toMinute': start_minute}
            ] %}
          {% endif %}

          {{ {
            'break_even_cost_to_charge': 0,
            'mode': 'time_of_use',
            'tariff_content_v2': {
              'code': 'MANUAL-IMPORT-ISOLATED-SPIKE',
              'currency': 'GBP',
              'daily_charges': [{'amount': 0, 'name': 'Charge'}],
              'demand_charges': {'ALL': {'rates': {'ALL': 0}}},
              'energy_charges': {
                'ALL': {'rates': {'ALL': 0}},
                'AllYear': {'rates': {'OFF_PEAK': 0.00, 'ON_PEAK': 1.50}}
              },
              'name': 'Manual Force Import',
              'seasons': {
                'AllYear': {
                  'fromDay': 1, 'fromMonth': 1, 'toDay': 31, 'toMonth': 12,
                  'tou_periods': {
                    'OFF_PEAK': {'periods': off_peak_periods},
                    'ON_PEAK': {'periods': on_peak_periods}
                  }
                }
              },
              'sell_tariff': {
                'currency': 'GBP',
                'daily_charges': [{'amount': 0, 'name': 'Charge'}],
                'demand_charges': {'ALL': {'rates': {'ALL': 0}}},
                'energy_charges': {
                  'ALL': {'rates': {'ALL': 0}},
                  'AllYear': {'rates': {'OFF_PEAK': 0.00, 'ON_PEAK': 0.00}}
                },
                'name': 'Manual Force Import',
                'seasons': {
                  'AllYear': {
                    'fromDay': 1, 'fromMonth': 1, 'toDay': 31, 'toMonth': 12,
                    'tou_periods': {
                      'OFF_PEAK': {'periods': off_peak_periods},
                      'ON_PEAK': {'periods': on_peak_periods}
                    }
                  }
                },
                'utility': 'Octopus Energy'
              },
              'utility': 'Octopus Energy',
              'version': 1
            }
          } }}
    - delay:
        seconds: 15
    - action: switch.turn_on
      target:
        entity_id:
        - switch.rouse_house_pw3_allow_charging_from_grid
    - action: number.set_value
      data:
        value: 100
      target:
        entity_id:
        - number.rouse_house_pw3_backup_reserve
    - action: select.select_option
      data:
        option: pv_only
      target:
        entity_id:
        - select.rouse_house_pw3_allow_export
    - action: select.select_option
      data:
        option: autonomous
      target:
        entity_id:
        - select.rouse_house_pw3_operation_mode
    - delay:
        minutes: 3
    - if:
      - above: -2
        condition: numeric_state
        entity_id: sensor.powerwall_192_168_1_182_battery_power
      then:
      - action: select.select_option
        data:
          option: self_consumption
        target:
          entity_id:
          - select.rouse_house_pw3_operation_mode
      - delay:
          seconds: 45
      - action: select.select_option
        data:
          option: autonomous
        target:
          entity_id:
          - select.rouse_house_pw3_operation_mode
      - action: notify.send_message
        data:
          message: 'Manual Import: battery was not charging after 3m. Toggled Self-Powered -> 45s -> Autonomous to wake up
            the Tesla Gateway.'
          title: '⚡ Powerwall Watchdog: Kickstart Nudge Executed'
        target:
          entity_id: notify.pixel_10_pro_xl
  - conditions:
    - condition: trigger
      id:
      - stop
      - timer_finished
    sequence:
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.start_manual_force_import
    - action: timer.cancel
      target:
        entity_id: timer.import_countdown_timer
    - action: automation.trigger
      target:
        entity_id: automation.powerwall_daily_scheduler
      data:
        skip_condition: false
mode: restart
